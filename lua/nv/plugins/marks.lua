return {
  "chentoast/marks.nvim",
  opts = {
    default_mappings = false,
    mappings = {
      set_next = 'm,',
      next = 'm]',
      prev = 'm[',
      toggle = 'm;',
      delete_buf = 'dmb',
      preview = 'm:',
    }
  }
}
